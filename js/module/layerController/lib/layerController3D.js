const layerController3D = class{
    constructor(original_layer, baselayers, overlaylayers){
    }

    eventListener_for_baselayers = (value) => {
    }

    eventListener_for_overlaylayers = (value) => {
    }

    _update = () => {
    }        
}